{{! 4. Crear un parcial para la sección principal en donde se renderice un helper con el mensaje de bienvenida y se realice una iteración para renderizar un parcial “producto” pasándole como argumento el nombre de cada producto. }}

<div id="modal" class="modal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            {{#if usuario}}
            <div class="modal-header">
                <h5 class="modal-title">Bienvenid@: {{usuario}}</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <p>Tus compras hoy fueron las siguientes:</p>
                {{#each ventas}}
                <div class="col mb-4">
                    <img src="{{this}}.png" class="card-img-top" alt="{{this}}"
                        style="width: 30%; display: inline-block; margin: auto">
                        <a href="/eliminar/{{this}}">
                        ❌
                    </a>
                </div>
                {{/each}}
            </div>
            {{/if}}
            {{#unless usuario}}
            <div class="modal-body">
                <p class="text-center">¡Hola! Para agregar al carrito, ingresa a tu cuenta.</p>
            </div>
            {{/unless}}
        </div>
    </div>
</div>
</div>